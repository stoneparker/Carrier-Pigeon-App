import { StyleSheet } from 'react-native';

export default StyleSheet.create({
     bannerContainer: {
          // position: 'absolute',
          position: 'relative',
          top: '-20%',
          width: '100%',
          height: 300,
          resizeMode: 'cover',
          backgroundColor: 'red'
     }
})