import React from 'react';
import { View, Text, StyleSheet} from 'react-native';

export default function Saved() {
     return (
          <View style={{ flex: 1 }}>
               <Text>Saved</Text>
          </View>
     )
}
